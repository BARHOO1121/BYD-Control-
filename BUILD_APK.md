# Build Car X BYD Alpha 4

GitHub Actions builds the APK automatically after the Alpha 4 source is pushed to `main`.

Artifact: `CarX-BYD-Alpha4-QR-Themes`

Local Android Studio/Gradle command:

```bash
gradle :app:assembleDebug
```

APK output:

`app/build/outputs/apk/debug/app-debug.apk`
